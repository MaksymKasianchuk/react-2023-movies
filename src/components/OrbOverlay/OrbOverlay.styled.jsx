import styled from '@emotion/styled';

export const CanvasStyled = styled.canvas`
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: -1;  
    background: transparent;
`;